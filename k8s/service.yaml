apiVersion: v1
kind: Service
metadata:
  name: farm-health-api-service
  namespace: flox-farm
  labels:
    app: farm-health-api
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
  selector:
    app: farm-health-api
    